replicaCount: 1

image:
  repository: registry.fullstackjam.dev/flask-watchlist
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: flask-watchlist.fullstackjam.dev
  tlsSecret: flask-watchlist-tls-certificate
